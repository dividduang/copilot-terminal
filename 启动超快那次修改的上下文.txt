• 我继续按“新 pane 启动过程中的输入竞争”这条线查。用户的新反馈说明不是历史恢复逻辑，而是 brand new agent pane 在创建和 send-keys 注入启动命令的同一时段，被别的输入先塞进了 PTY。

• Running rg -n "\\u001b\[\?1;2c|ESC\[c|DA 响应|设备属性|send-keys|startup protocol|协议响应" docs src
  │ -S

• Running Get-Content -Path src/renderer/components/TerminalPane.tsx | Select-Object -Skip 580 -First
  │ 60

• Ran Get-Content -Path src/renderer/components/TerminalPane.tsx | Select-Object -Skip 580 -First 60
  └           return false;
            }
    … +56 lines
        const initResizeTimer = window.setTimeout(() => scheduleResize(), 100);
        void replayHistory();
  └       const response = await service.executeCommand({
            argv: ['select-layout', 'main-vertical'],
    … +76 lines
            sizes: [0.3, 0.7],
            children: [

• Ran rg -n "\\u001b\[\?1;2c|ESC\[c|DA 响应|设备属性|send-keys|startup protocol|协议响应" docs src -S
  └ docs\claude-code-tmux-compatibility.md:117:#### 10. `tmux send-keys -t <pane> <command> Enter`
    docs\claude-code-tmux-compatibility.md:297:- `tmux send-keys ...`
    … +78 lines
    src\renderer\components\__tests__\TerminalPane.history.test.tsx:258:
    expect(window.electronAPI.ptyWrite).not.toHaveBeenCalledWith('win-1', 'pane-1', '\u001b[?1;2c');

─ Worked for 1m 39s ──────────────────────────────────────────────────────────────────────────────────

• 仓库里的开发文档也把 split-window 后马上 send-keys 的顺序一致性单独标成高风险点了。我去看这一段，确
  认当前实现有没有“pane 一创建出来，渲染层就开始把协议响应写回 PTY，和 main 侧 send-keys 抢输入流”的竞
  态。

• Running Get-Content -Path docs/claude-code-agent-teams-tmux-dev-guide.md | Select-Object -Skip 380
  │ -First 80

• Running Get-Content -Path docs/claude-code-tmux-compatibility.md | Select-Object -Skip 460 -First 60

• Ran Get-Content -Path docs/claude-code-tmux-compatibility.md | Select-Object -Skip 460 -First 60
  └ - 管理虚拟 session / window / pane namespace
    - 调用现有 `ProcessManager`、`windowStore` 对应能力
    … +56 lines
    - 仅在开启后，为 pane 注入 fake tmux 环境
    - 或仅对“Claude 专用窗口 / pane”注入 fake tmux PATH

  └    - `AUSOME_TMUX_RPC`

    … +76 lines

    - `tmux-shim -> RPC -> TmuxCompatService`

• Ran Get-Content -Path docs/claude-code-agent-teams-tmux-dev-guide.md | Select-Object -Skip 220
  │ -First 80
  └ > 只要 Claude Code 看到的“结果”符合预期，就没必要 1:1 复制 tmux 的所有内部行为。

    … +76 lines

    - 不只看源码字符串，要看**运行时日志里的真实值**

──────────────────────────────────────────────────────────────────────────────────────────────────────

• 我又看到另一个更像根因的点：send-keys 现在是“pane 一有 pid 就直接写命令”，并没有等这个 shell 完成启
  动握手。这样 send-keys 写进去的 PowerShell 命令，和 xterm 生成的 DA 响应会直接竞争同一条 stdin。先确
  认这条初始化等待逻辑有没有漏掉。

• Running rg -n "check-pty-output|hasOutput|wait.*pty|wait.*shell|startup.*output|splitPane\(|
  │ split-window|WaitingForInput" src -S

• Running Get-Content -Path docs/打开终端慢问题/首次打开终端慢的根因排查结论.md -TotalCount 120

• Ran Get-Content -Path docs/打开终端慢问题/首次打开终端慢的根因排查结论.md -TotalCount 120
  └ # 打开终端慢的根因排查结论

    … +81 lines
    | earlyDataBuffers 数据丢失 | 数据确实到达了 xterm，但 PowerShell 本身没输出 |
    | PowerShell 启动本身慢 | 第一个窗格证明同样条件下可以秒开 |
          if (ptySubscriptionManager) {
    … +76 lines
              });
            }

• Ran rg -n "check-pty-output|hasOutput|wait.*pty|wait.*shell|startup.*output|splitPane\(|
  │ split-window|WaitingForInput" src -S
  └ src\preload\index.ts:21:  checkPtyOutput: (windowId: string, paneId: string) =>
    ipcRenderer.invoke('check-pty-output', { windowId, paneId }),
    … +114 lines
    src\renderer\api\__tests__\events.test.ts:36:    expect(callback).toHaveBeenCalledWith('win-1',
    WindowStatus.WaitingForInput);

──────────────────────────────────────────────────────────────────────────────────────────────────────

• 当前实现里 check-pty-output 这条能力根本没被用到。也就是说，split-window 创建完 shell 以后，send-
  keys 并不会等 shell 完成启动握手，就直接把 teammate 启动命令写进 stdin，这和你现在看到的“命令前面被
  塞了 ?1;2c”是高度一致的。

• Running Get-Content -Path src/main/services/TmuxCompatService.test.ts -TotalCount 160

• Running Get-Content -Path src/main/services/StatusDetector.ts -TotalCount 180
  └ /**
     * TmuxCompatService 单元测试
    … +156 lines

      describe('split-window', () => {

• Ran Get-Content -Path src/main/services/StatusDetector.ts -TotalCount 180
  └ import { WindowStatus } from '../../shared/types/window';

    … +152 lines
• Ran Get-Content -Path src/main/services/TmuxIntegration.test.ts | Select-Object -Skip 228 -First 120
  └       expect(result.stdout.trim()).toBe('%0');
        });
    … +116 lines
          const uniqueIds = new Set(paneIds);
          expect(uniqueIds.size).toBe(3);

• Ran Get-Content -Path src/main/services/TmuxCompatService.ts -TotalCount 80
  └ /**
     * TmuxCompatService 闂傚倸鍊峰ù鍥綖婢跺顩插ù鐘差儏绾惧潡寮堕崼顐簴濞存粏顫夌换娑㈠箣閻愯尙鍔伴
    … +86 lines
      private rpcServer: TmuxRpcServer;


─ Worked for 1m 12s ──────────────────────────────────────────────────────────────────────────────────

• 我准备在主进程 tmux 兼容层补一个“startup barrier”：新 pane 刚 spawn 出来时，send-keys 先等这个 shell
  真正吐出可见输出（或者超时）再注入命令。这样启动握手和 teammate 启动命令就不会再挤在同一个时间窗里。

• Running Get-Content -Path src/main/services/TmuxCompatService.ts | Select-Object -Skip 420 -First
  │ 120

• Running Get-Content -Path src/main/services/TmuxCompatService.ts | Select-Object -Skip 1720 -First
  │ 180

    … +116 lines
        const handle = await this.config.processManager.spawnTerminal({
          windowId,

  └ 85:  private paneSubscriptions: Map<string, () => void>;
    98:    this.paneSubscriptions = new Map();
    … +21 lines
    undefined);
    2780:          this.detachPaneRuntime(window.id, pane.id, pid);
  └         this.resizePaneInLayout(window.layout, resolved.paneId, widthRatio, heightRatio);
          });
    … +177 lines
          const targetInfo = TmuxCommandParser.parseTarget(target);
          if (targetInfo.type !== 'pane' || !targetInfo.paneId) {
  │ TmuxCompatService.ts -S
  └ 103:  async executeCommand(request: TmuxCommandRequest): Promise<TmuxCommandResponse> {
    141:          case TmuxCommand.SendKeys:
    142:            return this.handleSendKeys(parsed, request);
    1797:  private handleSendKeys(parsed: any, request: TmuxCommandRequest): TmuxCommandResponse {

• Ran Get-Content -Path src/main/services/TmuxCompatService.ts | Select-Object -First 180
  └ /**
     * TmuxCompatService 闂傚倸鍊峰ù鍥綖婢跺顩插ù鐘差儏绾惧潡寮堕崼顐簴濞存粏顫夌换娑㈠箣閻愯尙鍔伴
    … +185 lines

• Ran rg -n "onPaneData" src/main -S
  └ src/main\index.ts:205:    onPaneData: ({ windowId, paneId, data }) => {
    src/main\services\TmuxCompatService.ts:54:  onPaneData?: (payload: { windowId: string; paneId:
    … +3 lines
    src/main\services\TmuxCompatService.ts:491:        this.config.onPaneData?.({ windowId, paneId, d
    ata });

• Ran Get-Content -Path src/main/index.ts | Select-Object -Skip 180 -First 60
  └   autoSaveManager = new AutoSaveManagerImpl();

    … +56 lines

      tmuxCompatService.on('window-removed', (data: any) => {

─ Worked for 2m 25s ──────────────────────────────────────────────────────────────────────────────────

• 我开始改主进程的 send-keys 顺序控制和对应单测，不碰渲染层输入路径了。这次目标很具体：让新 pane 的启
  动握手先完成，再往同一个 stdin 里写 teammate 启动命令。

• Running Select-String -Path src/main/services/TmuxCompatService.ts -Pattern "destroy\(\): void"
  │ -Context 0,20

• Running Get-Content -Path src/main/services/TmuxCompatService.ts | Select-Object -Skip 88 -First 24

• Ran Get-Content -Path src/main/services/TmuxCompatService.ts | Select-Object -Skip 88 -First 24
  └     this.config = config;
        this.paneIdMap = new Map();
    … +20 lines
          });


• Ran Get-Content -Path src/main/services/TmuxCompatService.ts | Select-Object -Skip 70 -First 40
  └
      /** 闂傚倸鍊风粈渚€骞夐敓鐘冲仭闁靛／鍛厠闂佸湱铏庨崰鏍不閺嶃劎绡€濠电姴鍊绘晶鏇㈡煕濞嗗骏韬
    … +38 lines
            cwd: request.cwd,
            debugContext: request.debugContext,

• Ran Select-String -Path src/main/services/TmuxCompatService.ts -Pattern "destroy\(\): void" -Context
  │ 0,20
  └
    > src\main\services\TmuxCompatService.ts:634:  destroy(): void {
    … +28 lines
    闂傚倸鍊风粈渚€骞栭锔藉亱闁糕剝鐟ч惌鎾绘倵濞戞鎴﹀矗韫囨稒鐓熼柡鍐ㄥ€哥敮鍫曟⒒?   *


• Edited src\main\services\TmuxCompatService.ts (+12 -3)
    36   * TmuxCompatService 闂傚倸鍊搁崐鐑芥倿閿曗偓椤灝螣閼测晝鐓嬮梺鍓插亝濞叉﹢宕? */
    37 -export interface TmuxCompatServiceConfig {
    37 +export interface TmuxCompatServiceConfig {
    38    /** ProcessManager 闂傚倷娴囬褎顨ョ粙鍖¤€块梺顒€绉寸壕缁樹繆椤栨艾鎮戞い?*/
       ⋮
    56    /** 闂傚倸鍊风粈渚€骞栭銈傚亾濮樺崬鍘寸€规洝顫夌€靛ジ寮堕幋鐘垫毎濠电偞鎸婚崺鍐磻閹惧灈鍋
        撶憴鍕８闁稿海鏁婚妴浣糕槈濮楀棛鍙嗛梺鍛婃处閸撴岸顢樿ぐ鎺撯拺闁煎鍊曞瓭濡炪倖鍨甸幊妯虹暦椤
        栫偛绠柤鎭掑劚閸撱劌顪冮妶鍡欏⒈闁稿绋撻悮鎯ь吋婢跺鍘繝銏ｆ硾閻楀棝宕濋妶鍥╃＜?*/
    57 -  debug?: boolean;
    58 -}
    57 +  debug?: boolean;
    58 +}
    59 +
    60 +type PaneStartupBarrier = {
    61 +  pid: number;
    62 +  windowId: string;
    63 +  paneId: string;
    64 +  createdAt: number;
    65 +  promise: Promise<void>;
    66 +  resolve: (reason: 'visible-output' | 'timeout' | 'disposed' | 'replaced') => void;
    67 +};
    68

• Edited src\main\services\TmuxCompatService.ts (+9 -6)
     92
     93 -  /** tmux 闂傚倸鍊风粈渚€骞夐敍鍕殰婵°倕鍟伴惌娆撴煙鐎电啸缁?pane 闂?PTY 闂傚倸鍊峰ù鍥ь浖
         閵娾晜鍤勯柤绋跨仛濞呯姵淇婇妶鍌氫壕闂佷紮绲介悘姘跺箯閸涘瓨鍊绘俊顖欒閳ь剚鐩娲濞戞氨鐤
         勯梺绋匡攻閻楃娀鏁?*/
     94 -  private paneSubscriptions: Map<string, () => void>;
     93 +  /** tmux 闂傚倸鍊风粈渚€骞夐敍鍕殰婵°倕鍟伴惌娆撴煙鐎电啸缁?pane 闂?PTY 闂傚倸鍊峰ù鍥ь浖
         閵娾晜鍤勯柤绋跨仛濞呯姵淇婇妶鍌氫壕闂佷紮绲介悘姘跺箯閸涘瓨鍊绘俊顖欒閳ь剚鐩娲濞戞氨鐤
         勯梺绋匡攻閻楃娀鏁?*/
     94 +  private paneSubscriptions: Map<string, () => void>;
     95 +  private paneStartupBarriers: Map<string, PaneStartupBarrier>;
     96 +  private readonly PANE_STARTUP_BARRIER_TIMEOUT_MS = 1200;
     97
        ⋮
    106        debug: config.debug,
    105 -    });
    106 -    this.paneMetadata = new Map();
    107 -    this.paneSubscriptions = new Map();
    108 -  }
    107 +    });
    108 +    this.paneMetadata = new Map();
    109 +    this.paneSubscriptions = new Map();
    110 +    this.paneStartupBarriers = new Map();
    111 +  }
    112

• Edited src\main\services\TmuxCompatService.ts (+13 -11)
    500
    501 -    if (this.config.onPaneData && this.config.processManager.subscribePtyData) {
    502 -      const unsubscribe = this.config.processManager.subscribePtyData(pid, (data: string) =>
         {
    503 -        this.config.onPaneData?.({ windowId, paneId, data });
    504 -      });
    501 +    if (this.config.onPaneData && this.config.processManager.subscribePtyData) {
    502 +      const unsubscribe = this.config.processManager.subscribePtyData(pid, (data: string) =>
         {
    503 +        this.releasePaneStartupBarrierOnOutput(paneId, data);
    504 +        this.config.onPaneData?.({ windowId, paneId, data });
    505 +      });
    506
        ⋮
    512     * 婵犵數濮烽弫鎼佸磻閻愬搫绠伴柟闂寸缁犵娀鏌熼悧鍫熺凡缂?pane 闂傚倷绀侀幖顐λ囬锕€鐤炬繝
         濠傜墕缁€澶嬫叏濡炶浜鹃梺闈涙缁舵岸鐛€ｎ喗鏅濋柍褜鍓涢悮鎯ь吋婢跺鍘鹃梺褰掓？缁€渚€鍩€椤
         掑啴鍝虹€垫澘瀚伴獮鍥敆閸屻倕鏁?   */
    512 -  private detachPaneRuntime(windowId: string, paneId: string, pid?: number): void {
    513 -    const unsubscribe = this.paneSubscriptions.get(paneId);
    513 +  private detachPaneRuntime(windowId: string, paneId: string, pid?: number): void {
    514 +    const unsubscribe = this.paneSubscriptions.get(paneId);
    515      if (unsubscribe) {
        ⋮
    518        } catch {}
    518 -      this.paneSubscriptions.delete(paneId);
    519 -    }
    520 -
    521 -    this.config.onPaneProcessStopped?.({ windowId, paneId, pid });
    522 -  }
    519 +      this.paneSubscriptions.delete(paneId);
    520 +    }
    521 +
    522 +    this.clearPaneStartupBarrier(paneId, 'disposed');
    523 +    this.config.onPaneProcessStopped?.({ windowId, paneId, pid });
    524 +  }
    525

• Edited src\main\services\TmuxCompatService.ts (+115 -11)
    559
    560 -    this.config.updateWindowStore((state: any) => {
    561 -      const window = state.windows.find((item: Window) => item.id === windowId);
    562 -      if (!window) {
    563 -        return;
    560 +    this.config.updateWindowStore((state: any) => {
    561 +      const window = state.windows.find((item: Window) => item.id === windowId);
    562 +      if (!window) {
    563 +        return;
    564        }
        ⋮
    568          pane.pid = handle.pid;
    569 -        pane.status = WindowStatus.WaitingForInput;
    570 -      }
    571 -    });
    572 -
    573 -    this.attachPaneRuntime(windowId, paneId, handle.pid);
    574 -    return handle.pid;
    575 -  }
    569 +        pane.status = WindowStatus.WaitingForInput;
    570 +      }
    571 +    });
    572 +
    573 +    this.registerPaneStartupBarrier(windowId, paneId, handle.pid);
    574 +    this.attachPaneRuntime(windowId, paneId, handle.pid);
    575 +    return handle.pid;
    576 +  }
    577 +
    578 +  private registerPaneStartupBarrier(windowId: string, paneId: string, pid: number): void {
    579 +    if (process.platform !== 'win32') {
    580 +      return;
    581 +    }
    582 +
    583 +    this.clearPaneStartupBarrier(paneId, 'replaced');
    584 +
    585 +    let settled = false;
    586 +    let resolvePromise!: () => void;
    587 +    const promise = new Promise<void>((resolve) => {
    588 +      resolvePromise = resolve;
    589 +    });
    590 +
    591 +    let barrier!: PaneStartupBarrier;
    592 +    const timeout = setTimeout(() => {
    593 +      barrier.resolve('timeout');
    594 +    }, this.PANE_STARTUP_BARRIER_TIMEOUT_MS);
    595 +
    596 +    barrier = {
    597 +      pid,
    598 +      windowId,
    599 +      paneId,
    600 +      createdAt: Date.now(),
    601 +      promise,
    602 +      resolve: (reason) => {
    603 +        if (settled) {
    604 +          return;
    605 +        }
    606 +
    607 +        settled = true;
    608 +        clearTimeout(timeout);
    609 +        this.paneStartupBarriers.delete(paneId);
    610 +        this.debugLog(undefined, 'pane startup barrier released', {
    611 +          windowId,
    612 +          paneId,
    613 +          pid,
    614 +          reason,
    615 +          waitedMs: Date.now() - barrier.createdAt,
    616 +        });
    617 +        resolvePromise();
    618 +      },
    619 +    };
    620 +
    621 +    this.paneStartupBarriers.set(paneId, barrier);
    622 +    this.debugLog(undefined, 'pane startup barrier registered', {
    623 +      windowId,
    624 +      paneId,
    625 +      pid,
    626 +      timeoutMs: this.PANE_STARTUP_BARRIER_TIMEOUT_MS,
    627 +    });
    628 +  }
    629 +
    630 +  private async waitForPaneStartupBarrier(windowId: string, paneId: string, request?: TmuxCom
         mandRequest): Promise<void> {
    631 +    if (process.platform !== 'win32') {
    632 +      return;
    633 +    }
    634 +
    635 +    const barrier = this.paneStartupBarriers.get(paneId);
    636 +    if (!barrier) {
    637 +      return;
    638 +    }
    639 +
    640 +    this.debugLog(request, 'waiting for pane startup barrier', {
    641 +      windowId,
    642 +      paneId,
    643 +      pid: barrier.pid,
    644 +      ageMs: Date.now() - barrier.createdAt,
    645 +    });
    646 +    await barrier.promise;
    647 +  }
    648 +
    649 +  private clearPaneStartupBarrier(
    650 +    paneId: string,
    651 +    reason: 'visible-output' | 'timeout' | 'disposed' | 'replaced',
    652 +  ): void {
    653 +    const barrier = this.paneStartupBarriers.get(paneId);
    654 +    if (!barrier) {
    655 +      return;
    656 +    }
    657 +
    658 +    barrier.resolve(reason);
    659 +  }
    660 +
    661 +  private releasePaneStartupBarrierOnOutput(paneId: string, data: string): void {
    662 +    if (!this.hasVisibleTerminalOutput(data)) {
    663 +      return;
    664 +    }
    665 +
    666 +    this.clearPaneStartupBarrier(paneId, 'visible-output');
    667 +  }
    668 +
    669 +  private hasVisibleTerminalOutput(data: string): boolean {
    670 +    if (!data) {
    671 +      return false;
    672 +    }
    673 +
    674 +    const withoutOsc = data.replace(/\x1b\][^\x07]*(?:\x07|\x1b\\)/g, '');
    675 +    const withoutCsi = withoutOsc.replace(/\x1b\[[0-?]*[ -/]*[@-~]/g, '');
    676 +    const withoutSingleEsc = withoutCsi.replace(/\x1b[@-_]/g, '');
    677 +    const withoutControlChars = withoutSingleEsc.replace(/[\x00-\x1f\x7f]/g, '');
    678 +    return withoutControlChars.trim().length > 0;
    679 +  }
    680

• Edited src\main\services\TmuxCompatService.ts (+12 -10)
    1914     */
    1915 -  private handleSendKeys(parsed: any, request: TmuxCommandRequest): TmuxCommandResponse {
    1915 +  private async handleSendKeys(parsed: any, request: TmuxCommandRequest): Promise<TmuxComman
          dResponse> {
    1916      try {
         ⋮
    1976        keys = this.adaptSendKeysForPane(keys, resolved.windowId, resolved.paneId, request);
    1977 -      this.debugLog(request, 'send-keys payload', {
    1978 -        originalPreview: originalKeys.slice(0, 300),
    1979 -        adaptedPreview: keys.slice(0, 300),
    1980 -        changed: originalKeys !== keys,
    1981 -      });
    1982 -
    1983 -      // 闂傚倸鍊风粈渚€骞夐敓鐘茬闁哄稁鍘介崑锟犳煏婢跺棙娅呴柣?PTY
    1984 -
    1985 -      this.config.processManager.writeToPty(pid, keys);
    1977 +      this.debugLog(request, 'send-keys payload', {
    1978 +        originalPreview: originalKeys.slice(0, 300),
    1979 +        adaptedPreview: keys.slice(0, 300),
    1980 +        changed: originalKeys !== keys,
    1981 +      });
    1982 +
    1983 +      await this.waitForPaneStartupBarrier(resolved.windowId, resolved.paneId, request);
    1984 +
    1985 +      // 闂傚倸鍊风粈渚€骞夐敓鐘茬闁哄稁鍘介崑锟犳煏婢跺棙娅呴柣?PTY
    1986 +
    1987 +      this.config.processManager.writeToPty(pid, keys);
    1988

• Edited src\main\services\TmuxCompatService.ts (+13 -9)
    751     * 闂傚倸鍊搁崐椋庣矆娴ｉ缚濮抽梺顒€绉寸壕濠氭煙閹呮憼闁告瑥绻橀弻銊モ攽閸℃浼€闂佸憡鏌ㄧ
         粔鐢垫崲濠靛鐒垫い鎺戝閻愬﹥銇勯幒鍡椾壕缂備讲鍋?   */
    752 -  destroy(): void {
    752 +  destroy(): void {
    753      // 闂傚倸鍊烽懗鍫曗€﹂崼銏″床闁瑰鍋熺粻鎯р攽閻樿弓杩?RPC 闂傚倸鍊风粈渚€骞栭锔藉亱闁糕
         剝鐟ч惌鎾绘倵濞戞鎴﹀矗韫囨稒鐓熼柡鍐ㄥ€哥敮鍫曟⒒?
        ⋮
    761      this.paneMetadata.clear();
    762 -    for (const unsubscribe of this.paneSubscriptions.values()) {
    763 -      try {
    764 -        unsubscribe();
    765 -      } catch {}
    766 -    }
    767 -    this.paneSubscriptions.clear();
    768 -    this.removeAllListeners();
    769 -  }
    762 +    for (const unsubscribe of this.paneSubscriptions.values()) {
    763 +      try {
    764 +        unsubscribe();
    765 +      } catch {}
    766 +    }
    767 +    this.paneSubscriptions.clear();
    768 +    for (const barrier of this.paneStartupBarriers.values()) {
    769 +      barrier.resolve('disposed');
    770 +    }
    771 +    this.paneStartupBarriers.clear();
    772 +    this.removeAllListeners();
    773 +  }
    774

• Edited src\main\services\TmuxCompatService.test.ts (+58 -22)
     10  // Mock ProcessManager
     11 -function createMockProcessManager(): ITmuxProcessManager {
     12 -  return {
     13 -    spawnTerminal: vi.fn().mockResolvedValue({ pid: 1001, pty: {} }),
     14 -    killProcess: vi.fn().mockResolvedValue(undefined),
     15 -    getProcessStatus: vi.fn().mockReturnValue(null),
     11 +function createMockProcessManager(): ITmuxProcessManager {
     12 +  return {
     13 +    spawnTerminal: vi.fn().mockResolvedValue({ pid: 1001, pty: {} }),
     14 +    killProcess: vi.fn().mockResolvedValue(undefined),
     15 +    getProcessStatus: vi.fn().mockReturnValue(null),
     16      listProcesses: vi.fn().mockReturnValue([]),
        ⋮
     18      subscribeStatusChange: vi.fn(),
     19 -    destroy: vi.fn().mockResolvedValue(undefined),
     20 -    getPidByPane: vi.fn().mockReturnValue(1001),
     21 -    writeToPty: vi.fn(),
     22 -  };
     23 -}
     19 +    destroy: vi.fn().mockResolvedValue(undefined),
     20 +    getPidByPane: vi.fn().mockReturnValue(1001),
     21 +    writeToPty: vi.fn(),
     22 +    subscribePtyData: vi.fn().mockReturnValue(vi.fn()),
     23 +  };
     24 +}
     25
        ⋮
    237
    237 -  describe('send-keys', () => {
    238 -    it('应向目标 pane 发送按键', async () => {
    239 -      const { service, processManager } = createService();
    240 -
    241 -      const response = await service.executeCommand({
    238 +  describe('send-keys', () => {
    239 +    it('应向目标 pane 发送按键', async () => {
    240 +      const { service, processManager } = createService();
    241 +
    242 +      const response = await service.executeCommand({
    243          argv: ['send-keys', '-t', '%1', 'echo hello', 'Enter'],
        ⋮
    245        });
    245 -
    246 -      expect(response.exitCode).toBe(0);
    247 -      expect(processManager.writeToPty).toHaveBeenCalledWith(1001, expect.stringContaining('e
         cho hello'));
    248 -    });
    249 -
    250 -    it('找不到 pane 时应返回错误', async () => {
    251 -      const { service } = createService();
    246 +
    247 +      expect(response.exitCode).toBe(0);
    248 +      expect(processManager.writeToPty).toHaveBeenCalledWith(1001, expect.stringContaining('e
         cho hello'));
    249 +    });
    250 +
    251 +    it('新建 pane 的 send-keys 应等待 shell 吐出可见输出后再注入命令', async () => {
    252 +      const { service, processManager } = createService();
    253 +      const ptySubscribers = new Map<number, (data: string) => void>();
    254 +      vi.mocked(processManager.subscribePtyData).mockImplementation((pid: number, callback: (
         data: string) => void) => {
    255 +        ptySubscribers.set(pid, callback);
    256 +        return vi.fn();
    257 +      });
    258 +
    259 +      const splitResponse = await service.executeCommand({
    260 +        argv: ['split-window', '-t', '%1', '-h', '-P', '-F', '#{pane_id}'],
    261 +        windowId: 'win-1',
    262 +        paneId: '%1',
    263 +      });
    264 +      const newPaneId = splitResponse.stdout.trim();
    265 +      vi.mocked(processManager.writeToPty).mockClear();
    266 +
    267 +      const sendKeysPromise = service.executeCommand({
    268 +        argv: ['send-keys', '-t', newPaneId, 'echo hello', 'Enter'],
    269 +        windowId: 'win-1',
    270 +      });
    271 +
    272 +      await Promise.resolve();
    273 +      expect(processManager.writeToPty).not.toHaveBeenCalled();
    274 +
    275 +      ptySubscribers.get(1001)?.('\u001b[c');
    276 +      await Promise.resolve();
    277 +      expect(processManager.writeToPty).not.toHaveBeenCalled();
    278 +
    279 +      ptySubscribers.get(1001)?.('PowerShell 7.5.4\r\n');
    280 +      const response = await sendKeysPromise;
    281 +
    282 +      expect(response.exitCode).toBe(0);
    283 +      expect(processManager.writeToPty).toHaveBeenCalledWith(1001, expect.stringContaining('e
         cho hello'));
    284 +    });
    285 +
    286 +    it('找不到 pane 时应返回错误', async () => {
    287 +      const { service } = createService();
    288

• 我补单测时发现一个实现细节也需要一起修：启动屏障不能依赖 onPaneData 回调存在，否则在一些路径下根本收
  不到释放信号。这个我一并收口到 attachPaneRuntime 里。

• Edited src\main\services\TmuxCompatService.ts (+4 -1)
    500
    501 -    if (this.config.onPaneData && this.config.processManager.subscribePtyData) {
    501 +    const shouldSubscribeToPtyData = this.config.processManager.subscribePtyData
    502 +      && (this.config.onPaneData || this.paneStartupBarriers.has(paneId));
    503 +
    504 +    if (shouldSubscribeToPtyData && this.config.processManager.subscribePtyData) {
    505        const unsubscribe = this.config.processManager.subscribePtyData(pid, (data: string) =>
         {

• Edited src\main\services\TmuxCompatService.test.ts (+1 -1)
    250
    251 -    it('新建 pane 的 send-keys 应等待 shell 吐出可见输出后再注入命令', async () => {
    251 +    it.runIf(process.platform === 'win32')('新建 pane 的 send-keys 应等待 shell 吐出可见输出
         后再注入命令', async () => {

• Ran npx tsc -p tsconfig.json --noEmit
  └ (no output)

• Ran npx tsc -p tsconfig.renderer.json --noEmit
  └ (no output)

• Updated Plan
  └ 根据你的复现反馈，我把根因判断从渲染层 history replay 改成了主进程 tmux `split-window -> send-
    keys` 的启动竞态，并已按这个方向实现修复。
    ✔ 在 TmuxCompatService 中为新建 pane 注册 startup barrier，避免 send-keys 与 shell 启动握手竞争
      stdin
    ✔ 让 attachPaneRuntime 在 barrier 存在时也订阅 PTY 数据，并在首个可见输出时释放 barrier
    ✔ 补充 Windows 回归测试并做 TypeScript 校验

─ Worked for 1m 31s ──────────────────────────────────────────────────────────────────────────────────

• 主进程这边已经改完并过了 TS 校验。我再把关键落点和测试位置标出来，然后给你一个最短的验证路径。

• Running Select-String -Path src/main/services/TmuxCompatService.ts -Pattern "pane startup barrier
  │ registered|waiting for pane startup barrier|hasVisibleTerminalOutput|
  │ registerPaneStartupBarrier|handleSendKeys\("

• Ran Select-String -Path src/main/services/TmuxCompatService.test.ts -Pattern "新建 pane 的 send-keys
  │ 应等待 shell 吐出可见输出后再注入命令"
  └
    src\main\services\TmuxCompatService.test.ts:251:    it.runIf(process.platform === 'win32')('新建
    pane
     的 send-keys 应等待 shell 吐出可见输出后再注入命令', async () => {


• Ran Select-String -Path src/main/services/TmuxCompatService.ts -Pattern "pane startup barrier
    … +17 lines
    CommandRequest): Promise<TmuxCommandResponse> {


  输入流，所以命令前面混进了 ?1;2c。修复在 TmuxCompatService.ts:576 到 TmuxCompatService.ts:672 和
  TmuxCompatService.ts:1922：新 pane spawn 后先注册一个 startup barrier，等 PTY 出现首个“可见输出”再执
  行 send-keys，避免 shell 启动握手和 teammate 启动命令并发写入。

  我也补了 Windows 回归测试 TmuxCompatService.test.ts:251，覆盖“新建 pane 时 send-keys 必须等 shell 有
  可见输出后再注入命令”这条路径。npx tsc -p tsconfig.json --noEmit 和 npx tsc -p

  现在请直接重新试一次 agent teams 创建。如果还会复现，我下一步就抓 copilot-terminal-tmux-debug.log 里
  pane startup barrier 和 send-keys payload 的实际时序。


› 很遗憾，问题仍然存在，请看截图：[Image #1]，右侧下面这个agent失败了，还是多了：?1;2c，第一个agent底
  部输入框也多了这个字符串。

  但是你的这次修改带来了一个非常大的好处！现在启动终端非常快。我希望在保证非常快的基础上，再分析下这
  个问题，看下这个字符串到底从哪里来的？